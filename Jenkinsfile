pipeline {
  agent any
  stages {
    stage('') {
      steps {
        input(message: 'Which deployment would you like to do?', ok: 'ok', submitter: 'submitter', submitterParameter: 'submitterparam', id: 'id')
      }
    }
  }
}